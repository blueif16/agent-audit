# Required environment variables — copy to .env and fill in values
# GOOGLE_API_KEY=        # Gemini Flash, Gemini Pro, Nano Banana (ranking, vision analysis, annotation, solution generation)
# FIRECRAWL_API_KEY=     # Firecrawl /map and /scrape (site discovery, page content extraction)
